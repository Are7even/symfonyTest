{% extends 'base.html.twig' %}

{% block title %}New Order{% endblock %}

{% block body %}
    <h1> Your Order</h1>
    <div class="col-md-4 order-md-2 mb-4">
        <h4 class="d-flex justify-content-between align-items-center mb-3">
            <span class="text-muted">Your cart</span>
            <span class="badge badge-secondary badge-pill">3</span>
        </h4>
        <ul class="list-group mb-3">
            {% for key, product in products %}

            <li class="list-group-item d-flex justify-content-between lh-condensed">
                <div>
                    <h6 class="my-0">{{ product.product.name }}</h6>
                </div>
                <span class="text-muted">${{ product.product.price }}</span>
                <span class="text-muted">x{{ product.quantity }}</span>
            </li>

            <li class="list-group-item d-flex justify-content-between">
                <span>Total (USD)</span>
                <strong>${{ product.quantity * product.product.price }}</strong>
            </li>
        </ul>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        {{ include('order/_form.html.twig', {'button_label': 'Complete order'}) }}
        {#        <form class="card p-2"> #}
        {#            <div class="input-group"> #}
        {#                <input type="text" class="form-control" placeholder="Promo code"> #}
        {#                <div class="input-group-append"> #}
        {#                    <button type="submit" class="btn btn-success">Complete order</button> #}
        {#                </div> #}
        {#            </div> #}
        {#        </form> #}
    </div>
{% endblock %}
